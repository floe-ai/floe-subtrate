# Issue #16 live QA evidence

Live QA ran against a temporary real bus on `127.0.0.1:5577` and FloeWeb on `127.0.0.1:5578`.

- `live-qa-summary.json` records filesystem checks, Field YAML payloads, workspace delete payloads, and native-dialog absence.
- Screenshots `01` through `07` show missing-directory, root Field, nested Field click/drop, duplicate inline error, and workspace delete checkbox dialogs.
