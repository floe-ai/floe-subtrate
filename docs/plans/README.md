# Plans

This directory contains working implementation plans proposed for specific slices. A plan captures the audit, proposed work items, and ordering for a slice at the time it was written.

These documents are point-in-time records, not current truth. Where they conflict with `CONTEXT.md` or accepted ADRs in `docs/adr/`, the canonical documents govern.
