# Floe Sample Project

This folder is intentionally plain. Running `floe` from a project like this
should open the add-workspace flow with the current path prefilled. Project
local `.floe/` files are created only after user consent through the product
flow.
